---
date: 2022-04-10
title: PDF extraction hacks
subtitle: PDF extraction hacks
description: "A collection of hacks and tips for extracting content from PDF files, including converting PDFs to images using the pdf2image library in Python, and other techniques for handling PDF data."
keywords: [pdf, pdf extraction, pdf2image, python, data extraction]
categories: [pdf]
---

# PDF extraction hacks

```python
#| label: pdf-2-png
#| fig-cap: "convert pdf to png"
from pdf2image import convert_from_path
pdf_path='in.pdf'

# Store Pdf with convert_from_path function
images = convert_from_path(pdf_path)
for i in range(len(images)):
    # Save pages as images in the pdf
    images[i].save('page'+ str(i) +'.png', 'PNG')
```